<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to "index" class="navbar-brand"}}Team Optimizer{{/link-to}}
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      {{#if session.isAuthenticated}}
        <ul class="nav navbar-nav">
          {{#link-to "groups" tagName="li"}}{{#link-to "groups"}}Groups{{/link-to}}{{/link-to}}
        </ul>
      {{/if}}
      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          <li><a class="clickable" {{action "invalidateSession"}}>Logout</a></li>
        {{else}}
          {{#link-to "register" tagName="li"}}{{#link-to "register"}}Register{{/link-to}}{{/link-to}}
          {{#link-to "login" tagName="li"}}{{#link-to "login"}}Log in{{/link-to}}{{/link-to}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
<div class="container">
  {{! Page content will go here }}
  {{outlet}}
</div>
<footer class="footer">
  <div class="container">
    <hr>
    <nav class="footerNav">
      <ul>
        <li>{{#link-to "about"}}About{{/link-to}}</li>
        <li>{{#link-to "contact"}}Contact{{/link-to}}</li>
      </ul>
    </nav>
  </div>
</footer>
